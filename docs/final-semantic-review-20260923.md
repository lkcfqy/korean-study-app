# 课程收尾定向复核（2026-09-23）

基线为已发布提交 `6d00012c`。先逐关复核当时仍匹配课程输入的 **625 条旧模型候选记录**（其中共有 1,451 个翻译、词义、注解和题目疑点），再按容易误选的同形词与固定搭配检索正式课程。候选记录只是定位线索；未运行本地语义模型，也没有直接采用模型建议。判定依据是韩文对话、题目选项及项目保存的国语院韩中词典。许多候选是风格建议、已正确翻译的内容或对题目干扰项的误报。

相对基线，最终生成的课程中 **208 关**有内容变化：**14 句中文、224 处点词显示义、154 处词典来源选择、10 处词形改正后的原形读音链接**。这 10 处只改变原形音频指向，表面词形音频仍对应原句；所需音频原本就在清单中，没有生成或替换 MP3。16 道题的选项文本随 14 句翻译及 2 道干扰项修订同步变化。两道修订题位于 `n26546-1-7`，原来“最后冲线”和“最后一名”均可作同一题答案。课程韩文、发言 ID 与顺序、说话人、学习日和正确答案位置均未改变。

## 代表性实质修订

| 课程 | 原问题 | 修订 |
| --- | --- | --- |
| `n24948-1-8`、`n24092-1-5` | 数学题的 `푸는` 被解析为 `푸다`“舀”。 | 还原 `풀다`“解答”，词典原形读音同步指向 `풀다`。 |
| `n16158-1-4` | 选购母犬或公犬的两个 `살까` 被解析为 `살다`“活着”。 | 还原 `사다`“买”。 |
| `n80936-1-10` | “称自己为演员”的 `배우라` 被解析为 `배우다`“学习”。 | 还原名词 `배우`“演员”。 |
| `n77144-3-6`、`n64498-7-5` | “三十分钟”“五分钟”的 `분` 被显示为“位”或“粉末”。 | 改用时间单位“分、分钟”。 |
| `n75371-1-8` | `정형외과` 显示为易误解成整容科的“整形外科”。 | 写明“骨科、矫形外科”。 |
| `n83008-1-13` | “不可以随便丢弃”的 `안된다면서` 被当成 `안되다`“不顺”，拆开后 `안` 又误选成“里面”。 | 按否定副词 `안`＋`되다` 解释为“不可以”。 |
| `n71920-1-8` | 中文无依据地说“输给主力缺阵的对手”。 | 改为“在主力全部缺阵的比赛中输了”，不指定缺阵者属于哪方。 |

专项检索逐例检查了 `안`、`분`、`여자`、`푸다/풀다`、`배우/배우다`、`말`、`타다`、`살다/사다`、`쓰다`、`걷다` 等高风险词形，并修正另外发现的词义和固定搭配问题。该检索是风险定向检查，不能换算成全库每句已被独立审校。

## 验证范围与边界

重建后有 **4,006 关、8,156 句发言、44,985 个点词块**。全量结构、来源选择、词形读音引用与 **28,783 个本地音频文件的字节数和 SHA-256** 校验通过，缺失音频为 0；兼容检查确认学习日、发言顺序、韩文和题目答案位置保持不变。类型检查、Lint、20 个 Node 测试和 12 个 Python 测试通过。构建与站点发布另作为交付步骤验证。

**625 条是旧模型提出的候选记录，不是全库 8,156 句及 44,985 个词块的独立逐条认证。** 本批逐项裁决了这些候选并扩展了确定性专项检索，但没有证明全库没有其他语义错误。音频清单标记 `ko-KR-SunHiNeural`，文件完整性及重点样本可解码、非静音且无削波；这也不能代替母语听审。先前 5 组高优先级转写分歧仍需真实试听；详见[读音收尾核对](pronunciation-review-20260923.md)。全库母语/教师独立签审、自然读音听审和真实学习效果验证仍是未完成范围，原 **81/100** 只可视为暂定产品评价。
